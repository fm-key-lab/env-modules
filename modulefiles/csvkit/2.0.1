#%Module
##
## csvkit (v2.0.1)

proc ModulesHelp { } {
   puts stderr "
Adds csvkit to your path
Version: 2.0.1
"
}

module-whatis "Adds csvkit to your path\n"
module-whatis "Provides: csvkit <-- should edit to list executables"

set basedir [getenv "MODULE_BASEDIR"]

# TODO: Should be `$basedir`
if { $basedir eq "" } {
    puts stderr "Error: MODULE_BASEDIR environment variable is not set."
    exit 1
}
prepend-path PATH "${basedir}/csvkit/2.0.1/.venv/bin/"